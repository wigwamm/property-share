require 'spec_helper'

describe "availabilities/edit" do
  # before(:each) do
  #   @availability = assign(:availability, stub_model(Availability,
  #     :booked => false,
  #     :from => "MyString"
  #   ))
  # end

  # it "renders the edit availability form" do
  #   render

  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   assert_select "form[action=?][method=?]", availability_path(@availability), "post" do
  #     assert_select "input#availability_booked[name=?]", "availability[booked]"
  #     assert_select "input#availability_from[name=?]", "availability[from]"
  #   end
  # end
end
