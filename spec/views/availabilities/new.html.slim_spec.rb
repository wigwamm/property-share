require 'spec_helper'

describe "availabilities/new" do
  # before(:each) do
  #   assign(:availability, stub_model(Availability,
  #     :booked => false,
  #     :created_by => "MyString"
  #   ).as_new_record)
  # end

  # it "renders new availability form" do
  #   render

  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   assert_select "form[action=?][method=?]", availabilities_path, "post" do
  #     assert_select "input#availability_booked[name=?]", "availability[booked]"
  #     assert_select "input#availability_created_by[name=?]", "availability[created_by]"
  #   end
  # end
end
