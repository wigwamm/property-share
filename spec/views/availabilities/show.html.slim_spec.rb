require 'spec_helper'

describe "availabilities/show" do
  # before(:each) do
  #   @time = Time.now
  #   @availability = assign(:availability, stub_model(Availability,
  #     :start_time => @time,
  #     :end_time => @time + 30.minutes,
  #     :booked => false,
  #     :created_by => "View Test"
  #   ))
  # end

  # it "renders attributes in <p>" do
  #   render
  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   rendered.should match(/#{@time.to_s}/)
  #   rendered.should match(/#{(@time + 30.minutes).to_s}/)
  #   rendered.should match(/false/)
  #   rendered.should match(/View Test/)
  # end
end
